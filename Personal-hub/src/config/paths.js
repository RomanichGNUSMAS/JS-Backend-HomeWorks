import path from 'node:path';

export const paths = {
    usersPath:path.resolve('../data/users.json'),
    habitsPath:path.resolve('../data/habits.json'),
    notesPath:path.resolve('../data/notes.json'),
    booksPath:path.resolve('../data/books.json')
}